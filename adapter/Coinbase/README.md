
This directory is an external adapter with the following main files:

test (unit tests)
index.js (where the external adapter is implemented)

It is recommended you test this using a Windows based machine. To test the adapter do the following terminal commands in the current directory:

1.npm install 

2. npm test


